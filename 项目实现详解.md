# 餐厅管理系统项目实现详解

## 1. 项目整体定位

这个项目本质上是一个前后端分离的餐厅管理系统，覆盖了餐厅日常运转中的几个核心环节：

- 员工登录与角色区分
- 桌台管理
- 原料分类与库存管理
- 菜品分类、菜品维护、菜品与原料绑定
- 顾客/服务员/前台下单
- 后厨制作队列
- 收款、催单、提权、上桌、清桌、光盘记录
- 经营概览与统计分析

从技术上看，它不是传统 JSP/Servlet 页面，而是：

- 后端：Spring Boot + MyBatis-Plus + MySQL
- 前端：Vue 3 + Vite
- 部署方式：前端打包后直接放到 Spring Boot 的 `static` 目录，由后端统一托管

也就是说，浏览器访问 `http://localhost:8080/` 时，拿到的是 Vue 打包后的静态页面；页面里的所有业务数据，再通过 `/api/...` 请求交给 Spring Boot 提供。

## 2. 项目结构与职责划分

### 2.1 目录结构

- `src/main/java/com/example/restaurantmanagement/`
  - 后端主程序、控制器、服务层、实体类、Mapper、鉴权与异常处理
- `src/main/resources/application.yml`
  - Spring Boot、数据库、MyBatis-Plus 配置
- `src/main/resources/static/`
  - Vue 前端打包后的静态资源
- `frontend/`
  - 独立前端工程
- `sql/restaurant_management_mysql8.sql`
  - 餐厅管理系统初始化脚本

### 2.2 分层关系

后端采用比较标准的分层：

- `Controller`
  - 负责接收 HTTP 请求，定义接口路径，做最外层的参数接入
- `Service`
  - 负责业务逻辑，是系统最核心的一层
- `Mapper`
  - 负责和数据库表直接打交道
- `Entity`
  - 对应数据库表结构
- `DTO`
  - 用于前后端之间的数据传输与视图封装

前端则比较集中，主要逻辑几乎都放在一个 `App.vue` 中：

- 页面切换
- 表单状态
- 请求调用
- 数据刷新
- 角色控制
- 弹窗与交互

这种写法的优点是演示型项目开发快、结构直观；缺点是单文件较大，后期维护时会比较重。

## 3. 前端是怎么实现的

## 3.1 前端启动方式

前端入口非常简单：

- `frontend/src/main.js`
  - 创建 Vue 应用并挂载到 `#app`
- `frontend/src/App.vue`
  - 整个系统的主界面和主要业务逻辑都在这里

Vite 配置见 `frontend/vite.config.js`，关键点有两个：

- 开发时把 `/api` 代理到 `http://localhost:8080`
- 构建时把产物直接输出到 `../src/main/resources/static`

这意味着：

1. 开发阶段可以 `npm run dev`，通过代理解决跨域问题。
2. 部署阶段不需要单独部署前端服务器，Spring Boot 自己就能把页面和接口一起提供出去。

## 3.2 前端页面组织方式

这个前端不是 Vue Router 多页面，而是“单页面 + 多模块切换”的实现方式。

在 `App.vue` 开头，定义了页面区块名：

- `customer` 顾客点餐
- `overview` 经营总览
- `auth` 员工登录
- `tables` 桌台管理
- `materials` 原料库存
- `dishes` 菜单中心
- `orders` 订单工作台
- `kitchen` 后厨看板
- `analytics` 经营分析

然后用 `activeSection` 控制当前显示哪个模块。模板里通过：

```vue
<section v-show="activeSection === 'orders'">
```

这种方式切换视图，所以本质上是一个 SPA 控制多个工作区。

## 3.3 前端状态管理方式

项目没有引入 Pinia 或 Vuex，而是直接使用 Vue 3 的组合式 API：

- `ref`
  - 保存基础状态，比如当前页面、是否加载中、当前用户
- `reactive`
  - 保存表单对象和消息对象
- `computed`
  - 计算当前角色权限、可见菜单、筛选后的订单等
- `watch`
  - 监听登录状态和可见菜单，自动修正当前页面

核心状态大致包括：

- 用户状态：`currentUser`
- 页面状态：`activeSection`
- 提示状态：`message`
- 数据状态：`dashboard`、`tables`、`materials`、`dishes`、`orders`、`kitchenQueue`
- 表单状态：登录、注册、桌台、原料、菜品、订单、付款、提权、光盘记录等表单

这种实现说明前端作者采用的是“集中式本地状态”思路：所有状态都收口在 `App.vue`，减少组件拆分成本。

## 3.4 前端权限控制方式

前端做了两层权限控制。

第一层是菜单可见性控制。

`SECTION_ACCESS` 定义了每个模块哪些角色能看到，比如：

- `analytics` 只有 `OWNER`、`MANAGER`
- `kitchen` 只有 `OWNER`、`MANAGER`、`CHEF`
- `orders` 只有 `OWNER`、`MANAGER`、`WAITER`、`CASHIER`

然后通过 `visibleSections` 这个计算属性动态生成左侧菜单。

第二层是按钮级权限控制。

比如：

- `canPayOrders`
- `canPrioritizeOrders`
- `canStartCooking`
- `canRecordWaste`

模板中直接用这些布尔值控制按钮是否禁用，从而实现角色裁剪。

要注意，这只是前端限制，真正的权限校验仍然在后端完成。

## 3.5 前端登录状态是怎么保存的

登录成功后，前端把用户对象写进浏览器 `localStorage`：

- 键名：`restaurant-management-user`

后续刷新页面时，前端会先从本地读取这个对象，恢复登录状态。

这是一种轻量化实现，没有引入 JWT、Session，也没有服务端登录态。它更适合课设/演示系统，优点是实现简单，缺点是安全性较弱。

## 3.6 前端请求层是怎么封装的

`frontend/src/api.js` 是统一请求层。

它做了几件事：

1. 固定接口前缀为 `/api`
2. 统一设置 `Content-Type: application/json`
3. 从 `localStorage` 中读取当前用户角色
4. 自动把角色写到请求头 `X-User-Role`
5. 统一解析后端返回的 `ApiResponse`
6. 如果 HTTP 状态不对或业务 `code !== 200`，直接抛错

因此，页面层调用时非常简单，例如：

- `api.login(...)`
- `api.listOrders()`
- `api.payOrder(id, data)`
- `api.kitchenQueue()`

页面组件不需要重复写 `fetch` 细节。

## 3.7 前端如何初始化数据

页面挂载时执行 `initializePage()`，主要流程如下：

1. 先请求 `/api/health` 检查服务状态
2. 如果当前是游客：
   - 只加载桌台、菜品分类、菜品数据
3. 如果当前是员工：
   - 并发加载总览、分析、桌台、原料分类、原料、菜品分类、菜品、订单、后厨队列
4. 再为每个菜品额外加载其原料绑定关系

这里很关键的一点是 `refreshAll()` 用了 `Promise.all(...)` 并发拉数据，所以页面初始化效率比串行请求更高。

## 3.8 前端几个主要模块的实现思路

### 3.8.1 顾客点餐模块

顾客模式下不需要登录。

前端会：

- 拉取在售菜品和桌台列表
- 顾客选择桌台
- 把菜品加入购物单
- 输入备注
- 提交订单到 `/api/orders`

提交时，前端会把表单转换成后端需要的结构：

- `tableId`
- `source`
- `remark`
- `customerNote`
- `items`

其中 `items` 里既支持正常菜品，也支持定制菜。

### 3.8.2 订单工作台

这个模块是前端交互最复杂的部分。

它包含两部分：

- 左侧/上方：新建订单表单
- 下方：订单卡片列表

点击某个订单后，会用 `teleport` 打开一个详情弹窗，弹窗中可做这些操作：

- 查看订单项状态
- 催单
- 确认上桌
- 收款
- 提权
- 填写光盘记录

也就是说，订单工作台把“订单生命周期中的多个动作”都收在一个界面里了。

### 3.8.3 后厨看板

后厨模块读取的是 `/api/kitchen/queue` 返回的数据，而不是直接读订单表。

前端展示的是“订单项”视角，而不是“订单”视角。每张卡片表示一道待处理菜品，显示：

- 订单号
- 桌台
- 菜名
- 数量
- 特殊要求
- 等待时长
- 提权等级
- 催单次数

后厨可以执行：

- 开始制作
- 标记已出餐

服务员或后厨可以在订单详情中继续把菜标记为“已上桌”。

### 3.8.4 经营分析

经营分析只对店长和经理开放。

前端直接展示后端已经算好的汇总数据：

- 累计营收
- 已结单订单数
- 已上桌菜品数
- 已记录光盘项数
- 浪费率
- 热销菜品
- 高浪费菜品
- 低库存原料

也就是说，这一页前端主要负责展示，统计逻辑在后端。

## 4. 后端是怎么实现的

## 4.1 启动与基础配置

主启动类是：

- `RestaurantManagementApplication`

它通过 `@MapperScan("com.example.restaurantmanagement.mapper")` 扫描所有 MyBatis-Plus Mapper。

`application.yml` 中主要配置了：

- 端口 `8080`
- MySQL 数据源
- Thymeleaf 关闭缓存
- MyBatis-Plus 驼峰映射
- SQL 日志输出

虽然依赖里保留了 Thymeleaf，但这个项目当前主要页面已经转成 Vue 前端，Thymeleaf 并不是主要实现方式。

## 4.2 实体类对应哪些表

从实体类可以看出数据库核心模型：

- `User`
  - 员工账号与角色
- `DiningTable`
  - 桌台信息
- `DishCategory`
  - 菜品分类
- `Dish`
  - 菜品基础信息
- `MaterialCategory`
  - 原料分类
- `MaterialStock`
  - 原料库存
- `DishMaterial`
  - 菜品与原料的绑定关系
- `RestaurantOrder`
  - 订单主表
- `OrderItem`
  - 订单明细表
- `PlateWasteRecord`
  - 光盘/剩菜记录

这套模型设计是比较合理的，因为它把“订单头”和“订单项”拆开了，也把“菜品定义”和“库存原料”拆开了，便于做库存扣减和统计分析。

## 4.3 Controller 层做了什么

Controller 层非常薄，核心是“转发请求到 Service”。

主要控制器包括：

- `UserController`
  - 注册、登录
- `DiningTableController`
  - 桌台增删改查
- `MaterialController`
  - 原料分类、原料库存、库存状态刷新
- `DishController`
  - 菜品分类、菜品、配料绑定
- `RestaurantOrderController`
  - 订单创建、收款、提权、结单、光盘记录
- `KitchenController`
  - 后厨队列、开始制作、出餐、上桌、催单
- `DashboardController`
  - 总览指标
- `AnalyticsController`
  - 经营分析

接口路径设计也比较统一，基本都挂在 `/api` 下，前端很好接。

## 4.4 Service 层为什么是核心

这个项目真正的业务价值主要在 `RestaurantOrderServiceImpl`。

因为餐厅系统最难的不是简单增删改查，而是“状态流转和联动”：

- 下单时要不要扣库存
- 哪些角色能操作
- 收款后订单状态怎么变
- 菜品全部上桌后订单怎么变
- 光盘记录提交后桌台怎么释放
- 统计分析怎么算

这些逻辑全部集中在服务层处理，而不是散落在 Controller 中，这是正确的分层方式。

## 4.5 统一返回与异常处理

后端定义了统一返回结构 `ApiResponse<T>`：

- `code`
- `message`
- `data`

正常时返回 `200 / success / data`，异常时返回 `500 / 错误信息 / null`。

配套的 `GlobalExceptionHandler` 负责统一兜底：

- 业务异常 `BusinessException`
- 参数校验异常
- 其他未知异常

这样前端只需要按统一格式解析，不用为每个接口单独处理结构差异。

## 5. 后端权限控制是怎么做的

## 5.1 角色注解

项目自定义了一个注解：

- `@RequireRoles({...})`

哪个接口只能让哪些角色访问，就标在类或方法上。

例如：

- 订单收款：`OWNER`、`MANAGER`、`CASHIER`
- 提权：`OWNER`、`MANAGER`
- 光盘记录：`OWNER`、`MANAGER`、`WAITER`

## 5.2 拦截器校验流程

后端通过 `RoleInterceptor` 拦截所有 `/api/**` 请求。

流程是：

1. 看当前 Controller 或方法上有没有 `@RequireRoles`
2. 如果没有，说明该接口公开访问
3. 如果有，就从请求头读取 `X-User-Role`
4. 为空则拒绝
5. 如果是 `OWNER`，直接放行
6. 否则判断当前角色是否包含在允许列表中

这套权限模型非常轻量，但能完成课设需要的“按角色分工”。

要注意，这里不是基于真正的登录凭证，而是基于请求头里的角色字段，所以更偏演示型权限控制，而不是生产级安全方案。

## 6. 核心业务交互是怎么串起来的

下面按业务链路讲项目最重要的前后端交互。

## 6.1 登录流程

### 前端

1. 用户填写登录表单
2. 前端调用 `api.login`
3. 成功后把返回的用户对象存入 `localStorage`
4. 根据角色跳转到默认页面
5. 再调用 `refreshAll()` 拉取全量业务数据

### 后端

1. `UserController.login` 接收请求
2. `UserServiceImpl.login` 根据用户名查库
3. 把用户提交的密码做 MD5
4. 和数据库中的密码比对
5. 成功后返回用户信息，并把密码字段置空

这一步只是“本地记住当前用户”，并没有建立服务端会话。

## 6.2 菜品与原料绑定流程

这个流程体现的是“菜品和库存之间的关系是如何建立的”。

### 前端

1. 菜单中心加载全部菜品
2. 再逐个请求 `/api/dishes/{id}/materials`
3. 打开“绑定配料”后编辑原料和用量
4. 提交到 `/api/dishes/{id}/materials`

### 后端

1. `DishController.bindMaterials` 接口接收请求
2. `DishServiceImpl.bindMaterials` 先删除该菜品旧绑定
3. 校验每个原料 ID 是否存在
4. 重新插入新的绑定数据

也就是说，这里采用的是“全量覆盖式绑定”，不是局部 diff 更新。好处是逻辑简单，坏处是每次保存都会先删后插。

## 6.3 下单流程

这是系统最核心的交互。

### 前端如何提交

顾客或员工填写订单表单后，前端调用：

- `POST /api/orders`

提交内容包括：

- 桌台 ID
- 下单来源
- 备注
- 顾客备注
- 菜品明细

订单明细支持两种情况：

- 普通菜品：`dishId`
- 定制菜：`customDishName + customPrice`

### 后端如何处理

`RestaurantOrderServiceImpl.createOrder()` 的逻辑非常完整：

1. 校验桌台是否选择
2. 校验订单是否至少有一项菜品
3. 校验桌台是否存在
4. 遍历订单项
5. 普通菜品要校验是否存在、是否在售
6. 查询该菜品绑定了哪些原料
7. 累加每种原料本次订单需要消耗多少
8. 统一检查库存够不够
9. 库存足够后，统一扣减原料库存
10. 计算订单总金额
11. 创建订单主表
12. 创建订单明细表
13. 把桌台状态改为 `DINING`

这里最值得注意的是第 8、9 步。它不是一边遍历一边扣，而是先算总需求，再整体校验，再整体扣减，这样更稳，不容易出现部分扣减成功、部分失败的状态污染。

另外，这个方法加了 `@Transactional`，说明整个下单过程在同一个事务里，只要中间任意一步失败，数据库都会回滚。

## 6.4 后厨处理流程

### 队列生成

后厨页面调用：

- `GET /api/kitchen/queue`

后端不是直接返回订单，而是从 `order_item` 中查出所有状态为：

- `PENDING`
- `COOKING`
- `READY`

的订单项，再组装成 `KitchenItemView`。

这使得后厨可以直接按“菜品任务”处理，而不是按订单整体处理。

### 状态流转

后厨相关动作包括：

- 开始制作：`PENDING -> COOKING`
- 标记已出餐：`COOKING -> READY`
- 确认上桌：`READY -> SERVED`

这些动作最终都落到 `RestaurantOrderServiceImpl.updateItemStatus()` 上统一处理。

如果状态不合法，例如想把一个还没开始做的菜直接改成已出餐，后端会直接抛出业务异常。

### 催单机制

催单操作不会改变订单项状态，只会：

- `urgeCount + 1`
- 记录 `lastUrgedTime`

之后这些数据会体现在后厨看板和订单详情里。

## 6.5 收款与订单状态流转

订单状态主要有：

- `PENDING_PAYMENT`
- `PAID`
- `WAITING_CLEAR`
- `COMPLETED`

菜品状态主要有：

- `PENDING`
- `COOKING`
- `READY`
- `SERVED`

这两个状态体系是分开的：

- 订单状态描述“整单”
- 菜品状态描述“每一道菜”

### 收款时

调用：

- `POST /api/orders/{id}/pay`

后端会：

1. 校验优惠金额是否合法
2. 计算最终应收金额
3. 写入支付方式
4. 记录付款时间
5. 判断当前订单的菜是否已经全部上桌

如果还没有全部上桌，订单状态变成：

- `PAID`

如果已经全部上桌，订单状态直接变成：

- `WAITING_CLEAR`

### 上桌后

如果某道菜被标记成 `SERVED` 后，发现这个订单全部菜都已经上桌，并且此前已经支付，那么后端会把订单状态改成 `WAITING_CLEAR`。

也就是说，“是否已支付”和“是否全部上桌”共同决定订单是否进入待清桌。

## 6.6 光盘记录与清桌流程

这个项目有一个比较有特色的点，就是把“光盘记录”纳入了订单闭环。

### 前端

只有当订单状态是：

- `WAITING_CLEAR`
- `COMPLETED`

时，前端才展示光盘记录区。

服务员或经理可以对每个已上桌菜品填写：

- `CLEAN`
- `LEFTOVER_SOME`
- `LEFTOVER_MUCH`

并附加备注。

### 后端

`recordWaste()` 会做这些事情：

1. 校验订单必须已经收款
2. 校验请求不能为空
3. 校验每条记录属于当前订单
4. 校验该订单项必须是 `SERVED`
5. 根据 `orderItemId` 查找是否已有光盘记录
6. 有则更新，没有则插入
7. 提交完成后把订单状态直接改成 `COMPLETED`
8. 释放桌台，把桌台状态改回 `IDLE`

因此，这个项目的“结单完成”不是单纯收款完成，而是：

收款 + 全部上桌 + 清桌/光盘记录完成

这是一个比较完整的业务闭环。

## 7. 数据统计是怎么做的

## 7.1 总览 Dashboard

总览接口会统计：

- 空闲桌台数
- 用餐中桌台数
- 活跃订单数
- 后厨待处理项数
- 低库存原料数
- 提权订单数
- 今日营收

这些统计都在 `RestaurantOrderServiceImpl.getDashboardOverview()` 里完成，前端拿到结果后直接展示卡片。

## 7.2 经营分析 Analytics

经营分析的统计逻辑也都在后端完成，主要包括：

- 总营收
- 已完成订单数
- 已上桌菜品数
- 已记录光盘项数
- 平均浪费率
- 热销菜品 Top5
- 高浪费菜品 Top5
- 低库存原料 Top5

其中“浪费率”的计算方式并不是按真实克重，而是按等级映射：

- `CLEAN = 0%`
- `LEFTOVER_SOME = 20%`
- `LEFTOVER_MUCH = 60%`

然后求平均值。

这是一种适合课程设计的近似统计方式，实现简单，也能体现分析思路。

## 8. 数据库初始化与兼容处理

SQL 脚本 `restaurant_management_mysql8.sql` 提供了完整的建表与初始化数据。

但后端还额外做了一层兼容：

- `DatabaseSchemaInitializer`

它在应用启动时自动执行：

1. 如果 `plate_waste_record` 表不存在，就创建
2. 尝试把 `order_item.dish_id` 改成允许为空
3. 尝试把 `plate_waste_record.dish_id` 改成允许为空

这样做的目的，是兼容“定制菜”这种没有标准菜品 ID 的场景，也兼容旧库未升级的情况。

## 9. 这个项目前后端交互的核心特点

我把这个项目的交互特点总结成 6 点。

### 9.1 统一接口前缀

前端全部调用 `/api/...`，后端也全部挂在 `/api` 下，结构清晰。

### 9.2 统一返回格式

所有接口都返回 `ApiResponse`，前端解析逻辑统一，报错处理也统一。

### 9.3 前端按角色裁剪界面

前端先做“显示层裁剪”，不同角色看到不同工作区和按钮。

### 9.4 后端再次做权限兜底

前端即使绕过了按钮限制，只要请求头角色不符合，后端还是会拦截。

### 9.5 业务动作会立即回刷全量数据

前端大多数操作成功后都会执行 `refreshAll()`，重新拉取最新数据。

这样的好处是：

- 数据一致性高
- 实现简单

代价是：

- 请求量偏大
- 前端不是局部更新，而是偏“全量重刷”

对于课程设计规模来说，这是很实用的折中方案。

### 9.6 交互围绕“订单主流程”展开

整个系统并不是零散的 CRUD 拼接，而是围绕一条完整业务线：

菜品/原料准备 -> 下单 -> 后厨制作 -> 上桌 -> 收款 -> 清桌 -> 光盘记录 -> 统计分析

这也是这个项目实现上最完整、最有价值的地方。

## 10. 这个实现方案的优点与不足

## 10.1 优点

- 技术栈简单，容易理解和演示
- 前后端职责清楚
- 订单业务闭环完整
- 库存、桌台、后厨、收款、光盘记录之间有联动
- Service 层承接了大部分核心业务逻辑
- 使用事务保证关键流程一致性
- 前端已经具备比较完整的工作台体验

## 10.2 不足

- 前端逻辑几乎都堆在 `App.vue`，文件过大
- 没有真正的认证体系，只有基于角色请求头的轻量鉴权
- 前端每次操作后大量全量刷新，扩展后性能会受影响
- 密码只做了 MD5，安全性有限
- 没有分页、搜索、乐观更新、缓存等更完整的工程化能力
- 统计分析是演示级口径，不是严格经营报表口径

## 11. 如果从答辩或汇报角度来讲，这个项目可以怎么概括

如果你要对老师或同学讲这个项目，可以用下面这段话概括：

这个项目采用 Spring Boot + MyBatis-Plus + Vue 3 + Vite 实现了一个前后端分离的餐厅管理系统。前端用单页工作台承载顾客点餐、员工登录、桌台、库存、菜单、订单、后厨和经营分析等模块，后端通过 Controller + Service + Mapper 分层实现业务。系统的核心在订单服务层：下单时会校验菜品和库存、预扣原料、生成订单和订单项，并联动桌台状态；后续又通过后厨状态流转、收款、催单、提权、光盘记录把整条餐厅业务链打通。前端和后端之间通过统一的 `/api` 接口、统一返回结构、角色请求头和全量刷新机制完成交互，整体实现比较完整，适合作为 Java Web/JavaEE 课程设计项目。

## 12. 一句话总结

这个项目不是简单把几个管理页面拼起来，而是围绕“订单驱动餐厅业务流转”来实现的：前端负责工作台和交互组织，后端负责业务规则、状态流转、库存扣减和统计汇总，二者通过统一接口层紧密配合。
