# 餐厅管理系统开发文档

## 1. 文档目的

本文档用于记录当前项目从“家庭食谱与食材库存管理系统”改造为“餐厅管理系统”的开发过程、当前完成情况、运行方式、已知问题和后续计划。  
重点是用于继续开发、阶段性交接和课程答辩准备，不是纯需求文档。

## 2. 项目当前状态

### 2.1 当前项目名称

餐厅管理系统

### 2.2 当前技术栈

- 后端：Spring Boot 3.3.5
- ORM：MyBatis-Plus 3.5.7
- 数据库：MySQL 8
- 前端：Vue 3 + Vite 5
- 语言版本：Java 17
- 构建工具：Maven、npm

### 2.3 当前代码仓库状态

- 本地 Git 仓库已初始化
- 当前分支：`main`
- 当前首个提交：`7a5344d`
- 首个提交信息：`init restaurant management system`
- 当前工作区包含第二阶段改造内容，适合继续做一次阶段性 Git 备份

## 3. 改造背景

原项目是一个围绕家庭场景的食材/食谱系统，核心是：

- 注册登录
- 食材分类与库存管理
- 食谱管理
- 食谱绑定食材
- 做菜扣减库存
- 购物清单生成

当前课设已经改为餐厅管理系统，因此本项目采用“保留前后端技术框架，重建业务模型”的方式推进，而不是只做字段改名或页面改字。

## 4. 当前已完成工作

### 4.1 工程与配置

- 保留原有 Spring Boot + MyBatis-Plus + Vue 3 + Vite 框架
- 更新项目元数据为餐厅管理系统
- 调整数据库连接到 `restaurant_management`
- 初始化本地 Git 仓库
- 增加 `.gitignore`
- 增加数据库启动时自动补表逻辑，用于兼容旧数据库缺少新增表的情况

### 4.2 数据库改造

新增脚本：

- `sql/restaurant_management_mysql8.sql`

当前脚本包含核心表：

- `user`：员工用户表
- `dining_table`：桌台表
- `dish_category`：菜品分类表
- `dish`：菜品表
- `material_category`：原料分类表
- `material_stock`：原料库存表
- `dish_material`：菜品原料关联表
- `restaurant_order`：订单主表
- `order_item`：订单菜品表
- `plate_waste_record`：光盘记录表

其中 `order_item.dish_id` 和 `plate_waste_record.dish_id` 已调整为可空，用于支持服务员创建菜单外定制菜；标准菜单菜品仍正常关联 `dish` 表。

脚本已包含默认测试数据：

- 店长、经理、服务员、前台、后厨默认账号
- 默认桌台
- 默认菜品分类和原料分类
- 示例菜品与配料关系

### 4.3 后端改造

当前已完成后端核心能力：

- 员工注册、登录
- 总览统计
- 桌台管理
- 原料分类和原料库存管理
- 菜品分类、菜品管理和菜品配料绑定
- 订单创建
- 顾客未登录点餐
- 服务员菜单外定制菜下单
- 订单收款
- 订单提权
- 后厨制作流转
- 催单
- 服务员确认上桌
- 光盘记录
- 经营分析
- 轻量角色接口拦截

### 4.4 前端改造

前端已从原“食谱工作台”改成“餐厅工作台”，当前模块包括：

- 顾客点餐
- 经营总览
- 员工登录
- 桌台管理
- 原料库存
- 菜单中心
- 订单工作台
- 后厨看板
- 经营分析

另外已完成：

- 按角色裁剪导航
- 按角色禁用无权限按钮
- 登录后自动进入更符合角色职责的默认页面
- 未登录默认进入顾客点餐页，员工登录作为可选入口
- 订单工作台改为小卡片摘要，点击后用弹窗查看订单详情和执行操作
- 订单工作台增加状态筛选，默认查看今日待处理订单，避免已完成订单持续堆积
- 顾客端只允许选择标准菜单菜品，服务员端支持菜单外定制菜

### 4.5 基础验证情况

- 前端 `npm run build` 已多次通过
- 静态资源已成功输出到 `src/main/resources/static/`
- IDEA 中出现的 `long -> int` 编译问题已修复
- 当前项目已能够启动
- 当前已补充数据库启动时自动创建 `plate_waste_record` 表，避免旧库结构导致运行时报错
- 当前已修正经营分析中“少量剩余被误记为 100% 浪费”的统计口径问题
- 当前已完成顾客点餐默认入口、订单小卡片弹窗和服务员定制菜下单功能
- 前端 `npm run build -- --emptyOutDir` 已通过，静态资源已重新输出到后端 `static` 目录

## 5. 当前系统支持的业务流程

### 5.1 员工登录与注册

- 可使用默认账号 `admin / admin`
- 注册时支持填写角色字段
- 当前角色主要用于演示分工和接口控制，不是正式登录态鉴权

### 5.2 桌台管理

- 新增桌台
- 编辑桌台
- 删除桌台
- 查看桌台状态

### 5.3 原料库存管理

- 原料分类增删
- 原料库存增删改查
- 刷新库存状态
- 状态分为：
  - `0`：库存正常
  - `1`：低库存
  - `2`：已沽清

### 5.4 菜品管理

- 菜品分类增删
- 菜品增删改查
- 菜品启售/停售
- 菜品配料绑定

### 5.5 下单流程

- 顾客未登录时可直接进入顾客点餐页
- 顾客选择桌台后从标准菜单选择菜品
- 顾客端支持数量和口味备注，不开放菜单外定制菜
- 服务员/前台登录后可在订单工作台代客下单
- 服务员订单工作台支持菜单外定制菜，可填写定制菜名、单价、数量和备注
- 支持订单备注和顾客备注
- 下单时实时校验原料库存
- 下单成功后自动预扣原料库存
- 菜单外定制菜不绑定标准菜品原料，因此不会预扣原料库存，但会进入后厨制作队列

### 5.6 后厨处理流程

- 查看后厨制作队列
- 菜品状态流转：
  - `PENDING`：待制作
  - `COOKING`：制作中
  - `READY`：已出餐
  - `SERVED`：已上桌
- 当前流程职责划分为：
  - 后厨负责 `PENDING -> COOKING -> READY`
  - 服务员负责 `READY -> SERVED`
- 支持催单次数累计
- 经理可设置提权等级和提权原因

### 5.7 收款、清桌与完成流程

- 前台可录入支付方式
- 前台可录入优惠金额
- 订单支付后状态为：
  - 若仍有菜品未上桌，则为 `PAID`
  - 若全部已上桌，则为 `WAITING_CLEAR`
- 服务员在 `WAITING_CLEAR` 阶段记录光盘情况
- 光盘记录提交后，订单状态更新为 `COMPLETED`
- 桌台在清桌完成后释放为 `IDLE`

### 5.8 光盘记录

- 可对已上桌菜品记录光盘情况
- 当前支持三种值：
  - `CLEAN`：光盘
  - `LEFTOVER_SOME`：少量剩余
  - `LEFTOVER_MUCH`：较多剩余
- 支持备注
- 暂不支持拍照留证

### 5.9 经营分析

当前支持：

- 累计营收
- 已结单订单数
- 已上桌菜品数
- 光盘记录数
- 总体浪费率
- 热销菜品排行
- 高浪费菜品排行
- 低库存原料预警

当前浪费率统计口径已调整为加权统计：

- `CLEAN = 0%`
- `LEFTOVER_SOME = 20%`
- `LEFTOVER_MUCH = 60%`

因此“少量剩余”不再被错误统计为 `100%` 浪费。

### 5.10 当前角色工作台逻辑

- 未登录用户：默认进入顾客点餐页
- 服务员默认进入订单工作台
- 前台默认进入订单工作台，主要负责收款
- 后厨默认进入后厨看板
- 经理和店长默认进入经营总览
- 当前角色职责分工为：
  - 服务员：催单、确认上桌、清桌记录
  - 前台：收款
  - 后厨：制作、出餐
  - 经理：提权、经营干预、查看分析
  - 店长：查看全局经营数据

### 5.11 订单工作台交互

- 订单工作台默认展示“今日待处理”订单
- 支持按待结账、已支付、待清桌、今日已完成、全部历史筛选
- 订单列表以小卡片展示，只显示单号、桌台、创建时间、菜品份数和状态
- 点击订单小卡片后，以弹窗形式查看完整详情
- 弹窗内可完成催单、确认上桌、收款、提权、光盘记录等操作

## 6. 当前接口清单

### 6.1 用户接口

- `POST /api/users/register`
- `POST /api/users/login`

### 6.2 总览接口

- `GET /api/dashboard/overview`

### 6.3 桌台接口

- `GET /api/tables`
- `POST /api/tables`
- `PUT /api/tables/{id}`
- `DELETE /api/tables/{id}`

### 6.4 原料接口

- `GET /api/material-categories`
- `POST /api/material-categories`
- `DELETE /api/material-categories/{id}`
- `GET /api/materials`
- `POST /api/materials`
- `PUT /api/materials/{id}`
- `DELETE /api/materials/{id}`
- `POST /api/materials/refresh-status`

### 6.5 菜品接口

- `GET /api/dish-categories`
- `POST /api/dish-categories`
- `DELETE /api/dish-categories/{id}`
- `GET /api/dishes`
- `POST /api/dishes`
- `PUT /api/dishes/{id}`
- `DELETE /api/dishes/{id}`
- `GET /api/dishes/{id}/materials`
- `POST /api/dishes/{id}/materials`

### 6.6 订单接口

- `GET /api/orders`
- `POST /api/orders`
- `POST /api/orders/{id}/pay`
- `POST /api/orders/{id}/priority`
- `POST /api/orders/{id}/complete`
- `POST /api/orders/{id}/waste-records`

### 6.7 后厨接口

- `GET /api/kitchen/queue`
- `POST /api/kitchen/items/{id}/start`
- `POST /api/kitchen/items/{id}/ready`
- `POST /api/kitchen/items/{id}/serve`
- `POST /api/kitchen/items/{id}/urge`

### 6.8 分析接口

- `GET /api/analytics/summary`

## 7. 目录结构说明

```text
javaee sx
├─ frontend
│  ├─ src
│  │  ├─ App.vue
│  │  ├─ api.js
│  │  ├─ main.js
│  │  └─ style.css
│  ├─ package.json
│  └─ vite.config.js
├─ sql
│  └─ restaurant_management_mysql8.sql
├─ src
│  └─ main
│     ├─ java/com/example/restaurantmanagement
│     │  ├─ auth
│     │  ├─ common
│     │  ├─ config
│     │  ├─ controller
│     │  ├─ dto
│     │  ├─ entity
│     │  ├─ exception
│     │  ├─ mapper
│     │  ├─ service
│     │  └─ RestaurantManagementApplication.java
│     └─ resources
│        ├─ application.yml
│        └─ static
├─ README.md
├─ pom.xml
└─ 开发文档.md
```

说明：

- 当前后端包名已统一调整为 `restaurantmanagement`，与餐厅项目命名保持一致
- 原食谱系统部分代码和脚本仍保留在仓库中
- 当前前端主界面和新增主流程已转向餐厅管理业务

## 8. 数据库导入与启动方式

### 8.1 导入数据库脚本

推荐命令：

```bash
mysql -u root -p < "C:\Users\ZYX\Desktop\javaee sx\sql\restaurant_management_mysql8.sql"
```

或者在 MySQL 命令行执行：

```sql
source C:/Users/ZYX/Desktop/javaee sx/sql/restaurant_management_mysql8.sql;
```

### 8.2 配置文件

位置：

- `src/main/resources/application.yml`

当前默认配置：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_management?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
```

启动前需根据本机环境修改用户名和密码。

### 8.3 启动方式

如果 IDEA 已配置 Maven：

```bash
mvn spring-boot:run
```

或：

```bash
mvn clean package
java -jar target/restaurant-management-system-0.0.1-SNAPSHOT.jar
```

### 8.4 前端构建

```bash
cd frontend
npm install
npm run build
```

## 9. 当前已知问题与限制

### 9.1 当前限制

- 当前角色控制是演示级实现，前端通过请求头携带角色，不是真正的会话鉴权
- 顾客端仅支持标准菜单点餐，菜单外定制菜仅开放给服务员/员工代客下单
- 光盘拍照留证尚未实现
- 自动超时提醒尚未实现
- 经营分析暂不支持按日/周/月切换
- 经营分析暂不支持图表和报表导出

### 9.2 当前保留项

- 原食谱系统的部分实体、控制器和 SQL 仍在仓库中保留
- 这些内容当前不影响餐厅主流程运行，但后续可以考虑清理和统一命名

## 10. 下一步开发计划

### 10.1 第一优先级

- 将当前轻量角色控制升级为正式登录态权限控制
- 为光盘记录增加拍照留证
- 增加清桌完成后的桌台历史记录

### 10.2 第二优先级

- 增加按日/周/月筛选的经营分析
- 补充图表展示
- 增加经营报表导出
- 补充操作日志
- 增加更完善的订单历史查询和详情检索能力

### 10.3 第三优先级

- 增加超时催单提醒规则
- 清理旧的食谱系统残留模块
- 统一包名和模块命名

## 11. 推荐的下一开发任务

建议下一个直接做：

1. 经营分析按时间维度筛选
2. 光盘拍照留证
3. 正式登录态和权限控制

原因：

- 这三项最贴近当前系统剩余的高价值差距
- 能明显提升答辩展示效果
- 能把当前“演示系统”进一步推近“完整业务系统”

## 12. 阶段总结

当前系统已经完成从家庭食谱系统到餐厅管理系统的前两阶段改造，已经具备可演示主流程：

- 登录
- 顾客点餐
- 桌台管理
- 原料库存管理
- 菜品与配料配置
- 下单
- 服务员定制菜下单
- 后厨制作
- 催单
- 服务员确认上桌
- 前台收款
- 待清桌
- 光盘记录
- 经营分析

后续重点不再是基础 CRUD，而是继续补齐：

- 正式权限
- 光盘留证
- 分析维度
- 业务闭环细节

完成这些后，系统会更接近完整的餐厅管理课设。
